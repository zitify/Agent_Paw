name: agent_paw
description: Agent Paw mobile client for Android
publish_to: none
version: 0.5.17+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  flutter_markdown: ^0.7.3
  intl: ^0.19.0
  package_info_plus: ^8.1.0
  path_provider: ^2.1.3
  open_file: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
